# Copy to .env (git-ignored, loaded by direnv). Stage credentials only — never a production key.
# Enables live acceptance tests: TF_ACC=1 make test-acc
FLUENCE_API_KEY=
FLUENCE_ENDPOINT=https://api.stage.fluence.dev
